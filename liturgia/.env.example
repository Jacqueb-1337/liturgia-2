# Copy to .env and edit values before deploying
STRIPE_SECRET=sk_test_xxx
STRIPE_WEBHOOK_SECRET=whsec_xxx
STRIPE_PRICE_MONTHLY=price_xxx
STRIPE_PRICE_YEARLY=price_xxx
JWT_SECRET=change_this_to_a_random_secret
SITE_URL=https://yourdomain.com/licenses

# Database - MySQL preferred for hosting
DB_DRIVER=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=liturgia
DB_USER=jacqueb
DB_PASS=Caveman13!

# Optional: root credentials used only by init_db.php to create DB/user (not required on many hosts)
DB_ROOT_USER=
DB_ROOT_PASS=

# Fallback to SQLite if DB_DRIVER is not 'mysql'
DB_PATH=./data/licenses.sqlite

FROM_EMAIL=no-reply@yourdomain.com
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
