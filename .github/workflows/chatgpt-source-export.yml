name: Temporary ChatGPT source export

on:
  push:
    branches:
      - chatgpt-liturgia-asset-export-20260719
  pull_request:
    branches:
      - main
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  actions: read

jobs:
  export-source:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Create source archive
        run: zip -q -r liturgia-source.zip . -x '.git/*' 'liturgia-source.zip' '.github/workflows/chatgpt-source-export.yml' '.chatgpt-export-trigger'

      - name: Download official Electron runtime
        run: curl -fL --retry 3 -o electron-v25.9.8-linux-x64.zip https://github.com/electron/electron/releases/download/v25.9.8/electron-v25.9.8-linux-x64.zip

      - name: Upload source archive
        uses: actions/upload-artifact@v4
        with:
          name: liturgia-source-export
          path: liturgia-source.zip
          retention-days: 1
          compression-level: 0

      - name: Upload Electron runtime
        uses: actions/upload-artifact@v4
        with:
          name: electron-v25.9.8-linux-x64
          path: electron-v25.9.8-linux-x64.zip
          retention-days: 1
          compression-level: 0
